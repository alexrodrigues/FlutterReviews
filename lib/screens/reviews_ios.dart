import 'package:flutter/material.dart';

class ReviewsIos extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text("Reviews do ios"),
    );
  }
}
