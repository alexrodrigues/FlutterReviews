import 'package:flutter/material.dart';

class ReviewsAndroid extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text("Reviews do Android"),
    );
  }
}
